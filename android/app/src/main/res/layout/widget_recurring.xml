<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/widget_bg"
    android:clipToOutline="true">

    <LinearLayout
        android:id="@+id/recurring_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:alpha="0.8">
        
        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@android:drawable/ic_popup_sync"
            android:tint="#009688" />
            
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:text="Sắp đến hạn"
            android:textSize="12sp"
            android:textColor="#FFFFFF" />
    </LinearLayout>

    <TextView
        android:id="@+id/recurring_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/recurring_header"
        android:layout_marginTop="8dp"
        android:text="Chi phí định kỳ"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:singleLine="true"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/recurring_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/recurring_title"
        android:layout_marginTop="2dp"
        android:text="0₫"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#F44336" />

    <RelativeLayout
        android:id="@+id/progress_container"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_below="@id/recurring_amount"
        android:layout_marginTop="8dp"
        android:background="#33FFFFFF">
        <ProgressBar
            android:id="@+id/recurring_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:progressDrawable="@drawable/progress_bar_horizontal"
            android:progress="50"
            android:max="100" />
    </RelativeLayout>

    <TextView
        android:id="@+id/recurring_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/progress_container"
        android:layout_marginTop="6dp"
        android:text="Còn 0 ngày"
        android:textSize="11sp"
        android:textColor="#FFFFFF"
        android:alpha="0.8" />

</RelativeLayout>

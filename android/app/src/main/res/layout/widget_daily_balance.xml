<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/widget_bg">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="💰"
            android:textColor="#CCFFFFFF"
            android:textSize="14sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text="Chi tiêu hôm nay"
            android:textSize="12sp"
            android:textColor="#CCFFFFFF" />
    </LinearLayout>

    <!-- Main Value -->
    <TextView
        android:id="@+id/daily_remaining_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/header"
        android:layout_marginTop="12dp"
        android:text="0₫"
        android:textColor="#4CAF50"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Bottom Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical">

        <ProgressBar
            android:id="@+id/daily_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:progressDrawable="@drawable/progress_bar_horizontal"
            android:max="100"
            android:progress="0"
            android:layout_marginBottom="8dp" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/daily_spent_value"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Chi: 0₫"
                android:textColor="#99FFFFFF"
                android:textSize="10sp"
                android:layout_alignParentStart="true" />
            <TextView
                android:id="@+id/daily_limit_value"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="HM: 0₫"
                android:textColor="#99FFFFFF"
                android:textSize="10sp"
                android:layout_alignParentEnd="true" />
        </RelativeLayout>
    </LinearLayout>

    <!-- Hidden stub for Kotlin provider -->
    <TextView
        android:id="@+id/daily_remaining_label"
        android:layout_width="0dp"
        android:layout_height="0dp" />

</RelativeLayout>
